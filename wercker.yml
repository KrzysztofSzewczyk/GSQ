
box: debian
build:
  steps:
    - script:
        name: make-all
        code: |
          sudo apt-get update
          echo Y | sudo apt-get install build-essential
          echo Y | sudo apt-get install php7.0
          echo Y | sudo apt-get install composer
          chmod 777 build.sh
          bash build.sh
